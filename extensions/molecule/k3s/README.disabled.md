The test is disabled since it is not possible to deploy k3s under docker. Using the install_only flag does work (not start)
but than we miss configurations that should exist. So there is currently no way to do this beside vagrant.
